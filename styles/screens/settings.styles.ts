import styled from "styled-components/native";
import { Description } from "styles/shared";

export const AppVersionWrapper = styled.View`
    margin-top: auto;
    width: 100%;
    padding: 0 16px;
`;

export const AppVersionText = styled(Description)`
    font-size: 12px;
    opacity: 0.4;
    text-align: center;
`;
